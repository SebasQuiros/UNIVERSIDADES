# Este directorio contiene referencias al schema de Prisma y las migraciones.
#
# El schema principal está en:  backend/prisma/schema.prisma
# Las migraciones se generan en: backend/prisma/migrations/
#
# Para generar una nueva migración:
#   docker compose exec backend npx prisma migrate dev --name nombre_descriptivo
#
# Para ver el estado de migraciones:
#   docker compose exec backend npx prisma migrate status
